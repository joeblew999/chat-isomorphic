/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Per-backend state, attachments, transient caches. Never committed —
# contains real Signal store, identity keys, message history, etc.
.data/
